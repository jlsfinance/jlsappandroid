Perfect 👍
Ab clear ho gaya — **tumne app Capacitor se banayi hai** (Web + Android).

Main **exact bata raha hoon: kahan kya karna hai**, step-by-step.
Isko follow karoge to **upar–niche cut 100% fix** ho jayega.

---

# 🔴 CAPACITOR ME PROBLEM KYU AATI HAI

* Android status bar + navigation bar
* Web app **fullscreen chali jaati hai**
* Safe area ka padding automatically nahi lagta

---

# ✅ FINAL SOLUTION (CAPACITOR – GUARANTEED)

## 🥇 STEP 1 — Status Bar HEIGHT FIX (VERY IMPORTANT)

📍 **File:**
`android/app/src/main/res/values/styles.xml`

### ❌ AGAR AISA HAI

```xml
<style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
```

### ✅ ISKO AISA KAR DO

```xml
<style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
```

---

## 🥈 STEP 2 — SYSTEM WINDOW FIX

📍 **File:**
`android/app/src/main/java/.../MainActivity.java`

### ❌ AGAR KUCH NAHI HAI

Add this 👇

```java
import android.os.Bundle;
import androidx.core.view.WindowCompat;

@Override
protected void onCreate(Bundle savedInstanceState) {
  super.onCreate(savedInstanceState);
  WindowCompat.setDecorFitsSystemWindows(getWindow(), true);
}
```

👉 Ye line **status bar + navigation bar se content bachati hai**

---

## 🥉 STEP 3 — HTML / CSS ME SAFE AREA ADD KARO (MOST IMPORTANT)

📍 **File:**
`src/global.css`
or
`src/styles.css`

### ✅ YE CODE ADD KARO (COPY–PASTE)

```css
:root {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
}
```

### AGAR BODY USE KARTE HO

```css
body {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
}
```

---

## 🥇 STEP 4 — SIDEBAR KO SCROLLABLE BANAO

📍 **Sidebar HTML**

### ❌ GALAT

```html
<div class="side-menu">
```

### ✅ SAHI

```html
<div class="side-menu" style="overflow-y:auto; height:100vh;">
```

OR CSS:

```css
.side-menu {
  height: 100vh;
  overflow-y: auto;
}
```

---

## 🟢 STEP 5 — STATUS BAR COLOR MATCH KARO (OPTIONAL BUT PRO)

📍 **File:**
`capacitor.config.ts`

```ts
plugins: {
  StatusBar: {
    backgroundColor: "#5B4FE9",
    style: "LIGHT"
  }
}
```

---

# ✅ FINAL CHECKLIST (FOLLOW THIS)

✔ Step 1: Theme change
✔ Step 2: setDecorFitsSystemWindows(true)
✔ Step 3: CSS safe-area padding
✔ Step 4: Sidebar scroll

---

## 🔥 MERI GUARANTEE

Capacitor apps me **99% cut issue** sirf:

* `WindowCompat.setDecorFitsSystemWindows(true)`
* `env(safe-area-inset-*)`

se solve ho jata hai.

---

### 👉 AGAR TUM CHAHTE HO

* Tum apni **sidebar ka HTML/CSS** paste kar do
* Main **exact usi ke hisaab se fix code likh dunga**

Bas paste karo 👍
